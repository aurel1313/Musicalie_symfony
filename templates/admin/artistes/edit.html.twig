{% extends 'admin/base_admin.html.twig' %}

{% block body %}

    <h2 class="text-center">{{ app.request.get('_route') == "app_admin_artiste_update" ? "Modifier" : "Ajouter" }} un artiste</h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">{{ app.request.get('_route') == "app_admin_artiste_update" ? "Modifier" : "Ajouter" }}</button>
    {{ form_end(form) }}

{% endblock %}